[Unit]
Description=FanController daemon — applies fan curves in the background
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/fancontroller --daemon
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
